{% extends 'CartoFicheBundle::layout.html.twig' %}

{%block content%}
    <div id="wrap">
        <div class="titre">
            <h2>Création d'une catégorie</h2>
            <hr>
        </div>
        {# <form action="{{path('carto_fiche_tag_add')}}" method="post" id="tag-add-form"> #}
        {{ form_start(form, {"action": path('carto_fiche_tag_edit', { 'id': tag.id })} ) }}
        <p>{{ form_label(form.libelle) }}</p>
        <p>{{ form_widget(form.libelle) }}</p>
        <input type="submit" id="btn-sbmt" value="Envoyer">
        {{ form_end(form) }}
    </div>
{%endblock%}